/*
import {Container} from "./MainContainer.Styled";
import SearchBar from "../searchBar/SearchBar";

const MainContainer = ({children,title,searchBar}) => {
    return (
        <Container>
            <h2>{title}</h2>
            {searchBar??null}
            {children}
        </Container>
    )
}

export default MainContainer;
*/
