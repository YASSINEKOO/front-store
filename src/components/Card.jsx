/*
import React from "react";
import { CardWrapper, AppIcon, Title } from "./Card.Styled";

const Card = ({ title, icon }) => (
        <CardWrapper>
            <AppIcon src={icon} alt="App Icon"/>
            <Title>{title}</Title>
        </CardWrapper>
    );

export default Card;*/
